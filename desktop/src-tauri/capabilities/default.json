{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default desktop permissions for the Manpower Optimization app.",
  "windows": ["main"],
  "permissions": ["shell:allow-open", "store:default"]
}
