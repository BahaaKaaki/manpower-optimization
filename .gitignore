.DS_Store
__pycache__/
*.py[cod]
*$py.class
.Python
*.so
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.egg-info/
.eggs/
dist/
build/
*.egg

# Azure zip deploy artifact (generated by ./deploy.sh)
manpower-deploy.zip

# Excel temp lock files
~$*

# Local env
.env
.venv/
venv/
env/

# Dependencies (install with npm/pip)
node_modules/

# Vite cache
.vite/

# Rust / Cargo (Tauri): never commit build artifacts
target/

# Tauri CLI generated schemas (regenerated by cargo tauri dev/build)
**/src-tauri/gen/

# PyInstaller sidecar binaries copied by scripts/build_api_sidecar.sh (platform-named)
**/src-tauri/binaries/

# macOS packaging outputs
*.dmg
*.app/

# IDE / OS (keep editor settings local unless explicitly shared)
.idea/
.vscode/
*.swp
*~

# Windows
Thumbs.db
ehthumbs.db
Desktop.ini

# Temporary Office / spreadsheet locks (Excel may create hidden copies)
.~lock.*

# Locally-installed Claude / agent skills (per-developer, not shared)
.agents/
skills-lock.json

